# Config from an older schema version (27).
# The Rust binary should parse this without error.
schema_version: 27
http:
  address: 0.0.0.0:3000
users:
  - name: admin
    password: $2b$10$schema27_hash_here
auth_attempts: 5
dns:
  bind_hosts:
    - 0.0.0.0
  port: 53
  upstream_dns:
    - https://dns10.quad9.net/dns-query
  filtering_enabled: true
filters:
  - id: 1
    enabled: true
    url: https://adguardteam.github.io/AdGuardSDNSFilter/Filters/filter.txt
    name: AdGuard DNS filter
